/* Version History
 *
 * v0.12  - Added menu with [TRIANGLE]
 * v0.10  - Bug fixes, [SELECT] + [X] = Mount with WebMAN, [SELECT] + [O] = /dev_hdd0 or /dev_hdd0/packages, [X] = Copy using symbolic links, [START] = Copy without symbolic links, Exit to XMB by default. [R2] + [O] on root to exit to Game Launcher
 * v0.90  - Improved font display, improved size display, don't create new folder if OSK is canceled
 * v0.8   - Improved color scheme, added mount/unmount dev_blind with [TRIANGLE], prevents delete on ../ and root directory
 * v0.7   - Larger font, added ../ to avoid empty dir, changed buttons: [SQUARE] = Select / [START] = Copy
 * v0.6   - Integration with Game Launcher
 * v0.5.2 - Fix NTFS and ExFAT mount/unmount on both ports
 * v0.5.1 - Fix NTFS mount/unmount
 * v0.5.0 - Add NTFS support
 * v0.4.0 - Add library support
 */
#define SWVER "v0.12"
